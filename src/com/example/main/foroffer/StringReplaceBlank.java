package com.example.main.foroffer;

/**
 * 请实现一个函数，把字符串中的每个空格替换成"%20"，例如“We are happy.”，则输出“We%20are%20happy.”。
 */
public class StringReplaceBlank {
    public static void main(String[] args) {
        String content = "We are happy.";
        System.out.println(replaceBlank(content));
    }

    private static String replaceBlank(String content) {
//        String replace = content.replace(" ", "%20");
//        return replace;
        return "";
    }

}

